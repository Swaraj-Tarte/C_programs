#include <stdio.h>

// This code calculates area of circle.

int main()
{
    float radius;

    printf("Enter radius of the circle : \n");
    scanf("%f", &radius);

    printf("Area of the circle is %f", 3.14 * radius * radius);
    return 0;
}